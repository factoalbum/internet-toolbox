"use client";

import Link from "next/link";
import { ArrowUpRight, Search, X } from "lucide-react";
import { useRouter } from "next/navigation";
import { useEffect, useMemo, useRef, useState } from "react";
import { featuredTools, tools } from "@/lib/tools";

export const searchAliases: Record<string, string[]> = { loan: ["emi", "finance"], mortgage: ["emi", "loan"], tax: ["income tax", "gst"], salary: ["salary", "income"], interest: ["emi", "fd", "sip", "compound"], investment: ["sip", "ppf", "fd", "interest"], money: ["currency", "salary", "gst", "emi"], photo: ["image", "compress"], picture: ["image", "compress"], file: ["developer", "markdown", "html", "viewer", "image", "compress", "pdf"], "file viewer": ["developer", "markdown", "html", "code", "viewer"], "developer file": ["developer", "markdown", "html", "viewer"], "developer file viewer": ["developer", "markdown", "html", "viewer"], pdf: ["document", "similarity", "compare", "difference", "merge", "split", "extract", "metadata"], "pdf compare": ["document", "similarity", "pdf", "visual"], "pdf difference": ["pdf", "compare", "changes", "diff"], "pdf merge": ["pdf", "merger", "combine"], "pdf split": ["pdf", "splitter", "pages"], "pdf to image": ["pdf", "images", "render"], "image to pdf": ["images", "pdf", "convert"], similarity: ["document", "pdf", "image", "compare"], "document similarity": ["pdf", "compare", "similarity"], compare: ["diff", "text", "document", "pdf", "similarity", "image", "screenshot"], visual: ["pdf", "image", "screenshot", "compare"], screenshot: ["difference", "compare", "visual"], metadata: ["pdf", "image", "inspect"], dimensions: ["image", "size", "width", "height"], merge: ["pdf", "combine"], split: ["pdf", "pages"], extract: ["pdf", "text"], compressor: ["pdf", "image", "compress"], markdown: ["developer", "file", "viewer", "text"], readme: ["markdown", "developer", "file", "viewer"], html: ["developer", "file", "viewer", "entity"], "html file": ["developer", "file", "viewer"], "code file": ["developer", "file", "viewer", "coding"], config: ["developer", "file", "viewer", "yaml", "json", "xml"], source: ["developer", "file", "viewer", "code"], text: ["word", "character", "case"], coding: ["json", "base64", "uuid", "url", "html", "file", "viewer"], developer: ["json", "base64", "uuid", "url", "timestamp", "file", "viewer", "markdown", "html"], linkedin: ["caption", "message", "post", "writer"], post: ["caption", "message", "writer"], caption: ["message", "writer", "instagram"], message: ["writer", "birthday", "congratulations", "thank you", "email"], writing: ["writer", "message", "caption", "post"], writer: ["caption", "message", "post"], birthday: ["birthday", "message", "writer"], congratulations: ["congratulations", "message", "writer"], "thank you": ["thank you", "message", "writer"], "leave request": ["leave", "message", "writer"], "professional email": ["email", "message", "writer"], timezone: ["time zone", "time"], "time zone": ["timezone", "world clock", "time"], international: ["timezone", "time zone", "currency"], regex: ["regular expression", "pattern", "developer"], "regular expression": ["regex", "pattern", "developer"], jwt: ["json web token", "token", "developer", "security"], token: ["jwt", "json web token", "developer", "security"], csv: ["json", "spreadsheet", "data", "developer"], "json csv": ["json", "csv", "spreadsheet", "data"], "json to csv": ["json", "csv", "spreadsheet", "data"], diff: ["compare", "text", "changes"] };

export default function ToolSearch() {
  const [query, setQuery] = useState(""); const [activeIndex, setActiveIndex] = useState(-1); const [focused, setFocused] = useState(false); const inputRef = useRef<HTMLInputElement>(null); const router = useRouter(); const normalizedQuery = query.trim().toLowerCase();
  useEffect(() => { const handler = (event: KeyboardEvent) => { if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "k") { event.preventDefault(); inputRef.current?.focus(); } }; window.addEventListener("keydown", handler); return () => window.removeEventListener("keydown", handler); }, []);
  const matches = useMemo(() => { if (!normalizedQuery) return featuredTools.filter((tool) => tool.status === "live").slice(0, 6); const aliases = searchAliases[normalizedQuery] ?? []; const words = normalizedQuery.split(/\s+/).filter(Boolean); const terms = [normalizedQuery, ...aliases, ...words]; return tools.filter((tool) => tool.status === "live").map((tool) => { const haystack = `${tool.name} ${tool.slug.replaceAll("-", " ")} ${tool.description} ${tool.category}`.toLowerCase(); const score = terms.reduce((total, term) => total + (haystack.includes(term) ? (term === normalizedQuery ? 4 : 1) : 0), 0); return { tool, score }; }).filter(({ score }) => score > 0).sort((a, b) => b.score - a.score).slice(0, 6).map(({ tool }) => tool); }, [normalizedQuery]);
  const showResults = focused || Boolean(query); const clearSearch = () => { setQuery(""); setActiveIndex(-1); };
  function handleKeyDown(event: React.KeyboardEvent<HTMLInputElement>) { if (event.key === "Escape") { clearSearch(); inputRef.current?.focus(); return; } if (!matches.length) return; if (event.key === "ArrowDown") { event.preventDefault(); setActiveIndex((index) => (index + 1) % matches.length); } else if (event.key === "ArrowUp") { event.preventDefault(); setActiveIndex((index) => (index - 1 + matches.length) % matches.length); } else if (event.key === "Enter" && activeIndex >= 0) { event.preventDefault(); router.push(`/tools/${matches[activeIndex].slug}`); clearSearch(); } }
  return <div className="relative w-full" onBlur={(event) => { if (!event.currentTarget.contains(event.relatedTarget as Node | null)) setFocused(false); }}><div className="flex min-h-14 min-w-0 items-center gap-3 rounded-2xl border border-[#d9d5cc] bg-white px-4 py-3 shadow-[0_4px_18px_rgba(23,23,23,.05)] transition focus-within:border-[#171717] focus-within:ring-4 focus-within:ring-[#c8f169]/50"><Search className="shrink-0 text-black/35" size={20} aria-hidden="true" /><label htmlFor="tool-search" className="sr-only">Search tools</label><input ref={inputRef} id="tool-search" value={query} onChange={(event) => { setQuery(event.target.value); setActiveIndex(-1); }} onFocus={() => setFocused(true)} onKeyDown={handleKeyDown} placeholder="Search for a tool or task..." className="min-w-0 flex-1 bg-transparent text-base outline-none placeholder:text-black/35" autoComplete="off" role="combobox" aria-autocomplete="list" aria-expanded={showResults} aria-controls="tool-search-results" aria-activedescendant={activeIndex >= 0 ? `tool-result-${matches[activeIndex]?.slug}` : undefined} />{query ? <button type="button" onClick={clearSearch} className="rounded-md p-1 text-black/35 hover:bg-black/5 hover:text-black focus:outline-none focus:ring-2 focus:ring-[#c8f169]" aria-label="Clear search"><X size={17} /></button> : <span className="hidden rounded-md bg-black/5 px-2 py-1 font-mono text-[10px] text-black/35 sm:block">Ctrl K</span>}</div>{showResults && <div id="tool-search-results" role="listbox" aria-label={query ? "Tool search results" : "Popular tools"} className="absolute left-0 right-0 top-full z-50 mt-2 max-h-[min(60vh,420px)] overflow-y-auto rounded-2xl border border-[#ddd9d0] bg-white text-[#171717] shadow-[0_18px_45px_rgba(23,23,23,.14)]">{matches.length ? <><div className="border-b border-[#eeeae3] px-4 py-3 text-xs font-bold text-black/35">{query ? "TOOLS" : "POPULAR TOOLS"}</div><div className="divide-y divide-[#eeeae3]">{matches.map((tool, index) => { const Icon = tool.icon; return <Link key={tool.slug} id={`tool-result-${tool.slug}`} role="option" aria-selected={index === activeIndex} href={`/tools/${tool.slug}`} onMouseEnter={() => setActiveIndex(index)} onClick={() => { clearSearch(); setFocused(false); }} className={`group flex min-w-0 items-center gap-3 p-4 transition ${index === activeIndex ? "bg-[#f0f6df]" : "hover:bg-[#f7f7f4]"}`}><span className="flex size-9 shrink-0 items-center justify-center rounded-lg bg-[#edf5d8]"><Icon size={17} aria-hidden="true" /></span><span className="min-w-0 flex-1"><span className="block truncate text-sm font-bold">{tool.name}</span><span className="block truncate text-xs text-black/40">{tool.description}</span></span><ArrowUpRight size={15} className="shrink-0 text-black/25" aria-hidden="true" /></Link>; })}</div></> : <div className="p-5"><p className="font-bold">No tool found</p><p className="mt-1 text-sm text-black/45">Try PDF, compare, screenshot, image, merge, split, tax or JSON.</p></div>}</div>}</div>;
}
