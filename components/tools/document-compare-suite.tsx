"use client";

import { useState } from "react";
import { GitCompare, Upload } from "lucide-react";
import { PDFDocument } from "pdf-lib";

type Variant =
  | "pdf-difference-checker" | "pdf-visual-comparator" | "pdf-layout-checker" | "pdf-text-extractor"
  | "pdf-metadata-viewer" | "pdf-page-analyzer" | "pdf-merger" | "pdf-splitter" | "pdf-to-images"
  | "images-to-pdf" | "pdf-compressor" | "image-similarity-checker" | "screenshot-difference-checker"
  | "image-metadata-viewer" | "image-dimension-checker" | "document-compare";

type PdfInfo = { pages: number; width: number; height: number; text: string; items: Array<{ page: number; x: number; y: number; width: number; height: number; text: string }> };
const labels: Record<Variant, { title: string; description: string; action: string }> = {
  "pdf-difference-checker": { title: "Find PDF changes", description: "Compare readable text in two PDFs and see what was added or removed.", action: "Compare changes" },
  "pdf-visual-comparator": { title: "Compare PDF visuals", description: "Render matching PDF pages so you can inspect their appearance side by side.", action: "Compare visuals" },
  "pdf-layout-checker": { title: "Check PDF layout", description: "Compare page dimensions and text placement between two PDFs.", action: "Check layout" },
  "pdf-text-extractor": { title: "Extract PDF text", description: "Pull readable text from a PDF and copy or download it locally.", action: "Extract text" },
  "pdf-metadata-viewer": { title: "Inspect PDF metadata", description: "View page count, dimensions, file size and basic document information.", action: "Inspect PDF" },
  "pdf-page-analyzer": { title: "Analyze PDF pages", description: "See page dimensions, orientation and text density at a glance.", action: "Analyze pages" },
  "pdf-merger": { title: "Merge PDFs", description: "Combine multiple PDF files into one PDF directly in your browser.", action: "Merge PDFs" },
  "pdf-splitter": { title: "Split a PDF", description: "Choose the pages you want and create a new PDF from them.", action: "Split PDF" },
  "pdf-to-images": { title: "Convert PDF to images", description: "Render PDF pages as downloadable PNG images.", action: "Render pages" },
  "images-to-pdf": { title: "Turn images into PDF", description: "Combine JPG and PNG images into one PDF locally.", action: "Create PDF" },
  "pdf-compressor": { title: "Compress a PDF", description: "Create a smaller image-based copy for easier sharing and storage.", action: "Compress PDF" },
  "image-similarity-checker": { title: "Compare two images", description: "Measure visual similarity and compare image dimensions.", action: "Compare images" },
  "screenshot-difference-checker": { title: "Compare screenshots", description: "Compare two screenshots and highlight how closely they match.", action: "Find differences" },
  "image-metadata-viewer": { title: "Inspect image metadata", description: "View image format, dimensions, file size, transparency and orientation.", action: "Inspect image" },
  "image-dimension-checker": { title: "Check image dimensions", description: "Get exact dimensions, aspect ratio, orientation and megapixels.", action: "Check dimensions" },
  "document-compare": { title: "Compare documents", description: "Compare two PDF documents for readable content changes.", action: "Compare documents" },
};

async function loadPdf(file: File): Promise<PdfInfo> {
  const pdfjs = (await import("pdfjs-dist/legacy/build/pdf.mjs")) as any;
  const pdf = await pdfjs.getDocument({ data: new Uint8Array(await file.arrayBuffer()) }).promise;
  let text = ""; let width = 0; let height = 0; const items: PdfInfo["items"] = [];
  for (let pageNo = 1; pageNo <= pdf.numPages; pageNo += 1) {
    const page = await pdf.getPage(pageNo); const viewport = page.getViewport({ scale: 1 });
    width = Math.max(width, viewport.width); height = Math.max(height, viewport.height);
    const content = await page.getTextContent(); const pageItems = content.items as any[];
    text += `${pageItems.map((item) => item.str ?? "").join(" ")}\n`;
    for (const item of pageItems) { if (!item.str?.trim()) continue; items.push({ page: pageNo, x: item.transform?.[4] ?? 0, y: item.transform?.[5] ?? 0, width: item.width ?? 0, height: item.height ?? 0, text: item.str }); }
  }
  return { pages: pdf.numPages, width, height, text, items };
}
async function renderPdfPage(file: File, pageNo: number, scale = 0.75) {
  const pdfjs = (await import("pdfjs-dist/legacy/build/pdf.mjs")) as any;
  const pdf = await pdfjs.getDocument({ data: new Uint8Array(await file.arrayBuffer()) }).promise;
  const page = await pdf.getPage(Math.min(pageNo, pdf.numPages)); const viewport = page.getViewport({ scale });
  const canvas = window.document.createElement("canvas"); canvas.width = Math.ceil(viewport.width); canvas.height = Math.ceil(viewport.height);
  const context = canvas.getContext("2d"); if (!context) throw new Error("Your browser could not create a PDF preview.");
  await page.render({ canvasContext: context, viewport, canvas }).promise;
  return { dataUrl: canvas.toDataURL("image/jpeg", 0.78), width: viewport.width, height: viewport.height };
}
function asArrayBuffer(bytes: Uint8Array) { const copy = new Uint8Array(bytes.byteLength); copy.set(bytes); return copy.buffer; }
function downloadBytes(bytes: Uint8Array, name: string, type: string) { const url = URL.createObjectURL(new Blob([asArrayBuffer(bytes)], { type })); const anchor = window.document.createElement("a"); anchor.href = url; anchor.download = name; anchor.click(); URL.revokeObjectURL(url); }
function words(value: string) { return new Set(value.toLowerCase().replace(/[^\p{L}\p{N}\s]/gu, " ").split(/\s+/).filter(Boolean)); }
function similarity(a: string, b: string) { const left = words(a); const right = words(b); if (!left.size && !right.size) return 100; let common = 0; for (const word of left) if (right.has(word)) common += 1; return Math.round((common / Math.max(1, new Set([...left, ...right]).size)) * 100); }
function diffLines(a: string, b: string) { const left = a.split(/\r?\n/).map((line) => line.trim()).filter(Boolean); const right = b.split(/\r?\n/).map((line) => line.trim()).filter(Boolean); const rightSet = new Set(right); const leftSet = new Set(left); return { removed: left.filter((line) => !rightSet.has(line)).slice(0, 100), added: right.filter((line) => !leftSet.has(line)).slice(0, 100) }; }
function parsePageSelection(value: string, total: number) { const selected = new Set<number>(); for (const part of value.split(",")) { const [startRaw, endRaw] = part.trim().split("-"); const start = Number(startRaw); const end = Number(endRaw ?? startRaw); if (!Number.isInteger(start) || !Number.isInteger(end) || start < 1 || end < start || end > total) throw new Error(`Enter pages from 1 to ${total}.`); for (let page = start; page <= end; page += 1) selected.add(page - 1); } return [...selected].sort((a, b) => a - b); }
async function imageInfo(file: File) { const url = URL.createObjectURL(file); try { const image = await new Promise<HTMLImageElement>((resolve, reject) => { const value = new Image(); value.onload = () => resolve(value); value.onerror = () => reject(new Error("The image could not be read.")); value.src = url; }); return { width: image.naturalWidth, height: image.naturalHeight, aspect: (image.naturalWidth / Math.max(1, image.naturalHeight)).toFixed(3), megapixels: ((image.naturalWidth * image.naturalHeight) / 1000000).toFixed(2), type: file.type || "Unknown", size: file.size }; } finally { URL.revokeObjectURL(url); } }
async function preview(file: File) { const url = URL.createObjectURL(file); try { const image = await new Promise<HTMLImageElement>((resolve, reject) => { const value = new Image(); value.onload = () => resolve(value); value.onerror = () => reject(new Error("The image could not be previewed.")); value.src = url; }); const canvas = window.document.createElement("canvas"); const scale = Math.min(1, 900 / Math.max(image.naturalWidth, image.naturalHeight)); canvas.width = Math.max(1, Math.round(image.naturalWidth * scale)); canvas.height = Math.max(1, Math.round(image.naturalHeight * scale)); canvas.getContext("2d")?.drawImage(image, 0, 0, canvas.width, canvas.height); return canvas.toDataURL("image/jpeg", 0.76); } finally { URL.revokeObjectURL(url); } }
async function compareImages(a: File, b: File) { const [first, second] = await Promise.all([preview(a), preview(b)]); const [left, right] = await Promise.all([fetch(first).then((r) => r.blob()), fetch(second).then((r) => r.blob())]); const [leftBitmap, rightBitmap] = await Promise.all([createImageBitmap(left), createImageBitmap(right)]); const canvasA = window.document.createElement("canvas"); const canvasB = window.document.createElement("canvas"); canvasA.width = 96; canvasA.height = 96; canvasB.width = 96; canvasB.height = 96; const contextA = canvasA.getContext("2d"); const contextB = canvasB.getContext("2d"); if (!contextA || !contextB) throw new Error("Your browser could not compare these images."); contextA.drawImage(leftBitmap, 0, 0, 96, 96); contextB.drawImage(rightBitmap, 0, 0, 96, 96); const aData = contextA.getImageData(0, 0, 96, 96).data; const bData = contextB.getImageData(0, 0, 96, 96).data; let total = 0; for (let i = 0; i < aData.length; i += 4) { const delta = Math.abs(aData[i] - bData[i]) + Math.abs(aData[i + 1] - bData[i + 1]) + Math.abs(aData[i + 2] - bData[i + 2]); total += Math.max(0, 1 - delta / 765); } leftBitmap.close(); rightBitmap.close(); return Math.round((total / (96 * 96)) * 100); }
function Dropzone({ accept, multiple, files, onChange }: { accept: string; multiple?: boolean; files: File[]; onChange: (files: File[]) => void }) { return <label className="block cursor-pointer rounded-2xl border-2 border-dashed border-[#d3d0c6] bg-[#faf8f2] p-5 transition hover:border-[#171717] hover:bg-white"><input className="sr-only" type="file" accept={accept} multiple={multiple} onChange={(event) => onChange(Array.from(event.target.files ?? []))} /><div className="flex items-center gap-4"><span className="flex size-11 shrink-0 items-center justify-center rounded-xl bg-white text-[#536b1c] shadow-sm"><Upload size={19} /></span><div className="min-w-0"><p className="text-xs font-black uppercase tracking-[.12em] text-black/40">Choose {multiple ? "files" : "a file"}</p><p className="mt-1 truncate text-sm font-bold">{files.length ? files.map((file) => file.name).join(", ") : "Click here or drop your files"}</p><p className="mt-1 text-xs text-black/35">Processed locally in your browser</p></div></div></label>; }

export default function DocumentCompareSuite({ variant }: { variant: Variant }) {
  const meta = labels[variant]; const [files, setFiles] = useState<File[]>([]); const [result, setResult] = useState<Record<string, any> | null>(null); const [busy, setBusy] = useState(false); const [error, setError] = useState("");
  const pdf = variant.startsWith("pdf-") || variant === "document-compare"; const multiple = variant === "pdf-merger" || variant === "images-to-pdf"; const minimum = multiple ? (variant === "pdf-merger" ? 2 : 1) : ["pdf-text-extractor", "pdf-metadata-viewer", "pdf-page-analyzer", "pdf-splitter", "pdf-to-images", "pdf-compressor", "image-metadata-viewer", "image-dimension-checker"].includes(variant) ? 1 : 2;
  async function run() {
    setBusy(true); setError(""); setResult(null);
    try {
      if (files.length < minimum) throw new Error(`Choose ${minimum === 1 ? "a file" : "the two files"} first.`);
      if (files.some((file) => file.size > 20 * 1024 * 1024)) throw new Error("Each file must be 20 MB or smaller.");
      if (variant === "pdf-merger") { const out = await PDFDocument.create(); for (const file of files) { const source = await PDFDocument.load(await file.arrayBuffer()); const pages = await out.copyPages(source, source.getPageIndices()); pages.forEach((page) => out.addPage(page)); } downloadBytes(await out.save(), "merged.pdf", "application/pdf"); setResult({ message: `${files.length} PDFs merged successfully.` }); return; }
      if (variant === "images-to-pdf") { const out = await PDFDocument.create(); for (const file of files) { const bytes = await file.arrayBuffer(); const image = file.type === "image/png" ? await out.embedPng(bytes) : await out.embedJpg(bytes); const page = out.addPage([image.width, image.height]); page.drawImage(image, { x: 0, y: 0, width: image.width, height: image.height }); } downloadBytes(await out.save(), "images.pdf", "application/pdf"); setResult({ message: `${files.length} images added to the PDF.` }); return; }
      if (variant === "pdf-splitter") { const source = await PDFDocument.load(await files[0].arrayBuffer()); const value = window.prompt(`Pages to keep, for example 1-3,5. This PDF has ${source.getPageCount()} pages.`); if (!value) return; const indices = parsePageSelection(value, source.getPageCount()); const out = await PDFDocument.create(); const pages = await out.copyPages(source, indices); pages.forEach((page) => out.addPage(page)); downloadBytes(await out.save(), "split.pdf", "application/pdf"); setResult({ message: `${indices.length} pages exported.` }); return; }
      if (variant === "pdf-to-images") { const info = await loadPdf(files[0]); const images = []; for (let page = 1; page <= Math.min(info.pages, 12); page += 1) images.push(await renderPdfPage(files[0], page)); setResult({ images }); return; }
      if (variant === "pdf-compressor") { const info = await loadPdf(files[0]); const out = await PDFDocument.create(); for (let page = 1; page <= Math.min(info.pages, 24); page += 1) { const rendered = await renderPdfPage(files[0], page, 0.55); const bytes = await fetch(rendered.dataUrl).then((response) => response.arrayBuffer()); const image = await out.embedJpg(bytes); const target = out.addPage([rendered.width / 0.55, rendered.height / 0.55]); target.drawImage(image, { x: 0, y: 0, width: target.getWidth(), height: target.getHeight() }); } const bytes = await out.save(); downloadBytes(bytes, "compressed.pdf", "application/pdf"); setResult({ message: `Created a re-rendered PDF from ${Math.min(info.pages, 24)} pages.`, size: bytes.byteLength, original: files[0].size }); return; }
      if (!pdf) { const infoA = await imageInfo(files[0]); if (variant === "image-metadata-viewer" || variant === "image-dimension-checker") { setResult(infoA); return; } const infoB = await imageInfo(files[1]); setResult({ similarity: await compareImages(files[0], files[1]), widthA: infoA.width, heightA: infoA.height, widthB: infoB.width, heightB: infoB.height, sameDimensions: infoA.width === infoB.width && infoA.height === infoB.height, firstA: await preview(files[0]), firstB: await preview(files[1]) }); return; }
      const first = await loadPdf(files[0]);
      if (["pdf-text-extractor", "pdf-metadata-viewer", "pdf-page-analyzer"].includes(variant)) { if (variant === "pdf-text-extractor") { downloadBytes(new TextEncoder().encode(first.text), `${files[0].name.replace(/\.pdf$/i, "")}.txt`, "text/plain"); setResult({ text: first.text, pages: first.pages }); } else setResult({ pages: first.pages, width: Math.round(first.width), height: Math.round(first.height), orientation: first.width >= first.height ? "Landscape" : "Portrait", textLength: first.text.length, fileSize: files[0].size, averageTextPerPage: Math.round(first.text.length / Math.max(1, first.pages)) }); return; }
      const second = await loadPdf(files[1]);
      if (variant === "pdf-difference-checker" || variant === "document-compare") setResult({ similarity: similarity(first.text, second.text), ...diffLines(first.text, second.text) });
      else if (variant === "pdf-layout-checker") { const count = Math.min(first.items.length, second.items.length); let near = 0; for (let i = 0; i < count; i += 1) { const a = first.items[i]; const b = second.items[i]; if (a.page === b.page && Math.abs(a.x - b.x) <= 8 && Math.abs(a.y - b.y) <= 8) near += 1; } setResult({ contentSimilarity: similarity(first.text, second.text), layoutSimilarity: count ? Math.round((near / count) * 100) : 0, pagesA: first.pages, pagesB: second.pages, sizeA: `${Math.round(first.width)} × ${Math.round(first.height)}`, sizeB: `${Math.round(second.width)} × ${Math.round(second.height)}` }); }
      else { const firstPage = await renderPdfPage(files[0], 1); const secondPage = await renderPdfPage(files[1], 1); setResult({ similarity: similarity(first.text, second.text), pagesA: first.pages, pagesB: second.pages, firstA: firstPage.dataUrl, firstB: secondPage.dataUrl }); }
    } catch (err) { setError(err instanceof Error ? err.message : "Something went wrong. Try again."); } finally { setBusy(false); }
  }
  return <div className="p-4 sm:p-6 md:p-7"><div className="mb-5 flex items-start gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-[#c8f169] text-[#171717]"><GitCompare size={19} /></span><div><h2 className="text-lg font-black">{meta.title}</h2><p className="mt-1 text-sm leading-5 text-black/50">{meta.description}</p></div></div><Dropzone accept={pdf ? ".pdf,application/pdf" : "image/*"} multiple={multiple} files={files} onChange={(selected) => setFiles(selected.slice(0, multiple ? 10 : 2))} />{error && <p className="mt-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm font-semibold text-red-700" role="alert">{error}</p>}<button type="button" onClick={run} disabled={busy || files.length < minimum} className="mt-4 min-h-12 w-full rounded-xl bg-[#171717] px-5 text-sm font-black text-white transition hover:bg-black disabled:cursor-not-allowed disabled:opacity-35 focus:outline-none focus:ring-4 focus:ring-[#c8f169]">{busy ? "Working..." : meta.action}</button>{result && <div className="mt-6 space-y-4">{result.message && <div className="rounded-2xl border border-[#cfe0a5] bg-[#f0f8dc] p-4 text-sm font-bold">{result.message}</div>}{result.similarity !== undefined && <div className="grid gap-3 sm:grid-cols-3"><div className="rounded-2xl bg-[#171717] p-5 text-white"><p className="text-xs font-bold text-white/50">Similarity</p><p className="mt-1 text-4xl font-black">{result.similarity}%</p></div>{result.contentSimilarity !== undefined && <div className="rounded-2xl border border-[#ddd9cf] bg-white p-5"><p className="text-xs font-bold text-black/40">Content</p><p className="mt-1 text-2xl font-black">{result.contentSimilarity}%</p></div>}{result.layoutSimilarity !== undefined && <div className="rounded-2xl border border-[#ddd9cf] bg-white p-5"><p className="text-xs font-bold text-black/40">Layout</p><p className="mt-1 text-2xl font-black">{result.layoutSimilarity}%</p></div>}</div>}{result.text !== undefined && <textarea readOnly value={result.text} className="min-h-64 w-full resize-y rounded-2xl border border-[#ddd9cf] bg-[#faf8f2] p-4 font-mono text-xs leading-5 outline-none" aria-label="Extracted PDF text" />}{result.added && <div className="grid gap-3 md:grid-cols-2"><div className="rounded-2xl border border-[#d8e6b4] bg-[#f5fae8] p-4"><p className="text-xs font-black uppercase tracking-[.12em] text-[#58731d]">Added</p><ul className="mt-3 space-y-2 text-sm">{result.added.map((line: string) => <li key={line} className="break-words">+ {line}</li>)}</ul></div><div className="rounded-2xl border border-[#ead2d2] bg-[#fff7f7] p-4"><p className="text-xs font-black uppercase tracking-[.12em] text-[#9a3c3c]">Removed</p><ul className="mt-3 space-y-2 text-sm">{result.removed.map((line: string) => <li key={line} className="break-words">- {line}</li>)}</ul></div></div>}{result.firstA && <div className="grid gap-3 md:grid-cols-2"><div className="rounded-2xl border border-[#ddd9cf] bg-white p-3"><p className="mb-2 text-xs font-bold text-black/40">First file</p><img src={result.firstA} alt="First document preview" className="max-h-[520px] w-full rounded-lg object-contain" /></div><div className="rounded-2xl border border-[#ddd9cf] bg-white p-3"><p className="mb-2 text-xs font-bold text-black/40">Second file</p><img src={result.firstB} alt="Second document preview" className="max-h-[520px] w-full rounded-lg object-contain" /></div></div>}{result.images && <div className="grid gap-3 sm:grid-cols-2">{result.images.map((item: { dataUrl: string }, index: number) => <div key={item.dataUrl} className="rounded-2xl border border-[#ddd9cf] bg-white p-3"><p className="mb-2 text-xs font-bold text-black/40">Page {index + 1}</p><img src={item.dataUrl} alt={`PDF page ${index + 1}`} className="w-full rounded-lg" /></div>)}</div>}{result.pages && result.width && <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div className="rounded-2xl border border-[#ddd9cf] bg-white p-4"><p className="text-xs text-black/40">Pages</p><p className="mt-1 text-xl font-black">{result.pages}</p></div><div className="rounded-2xl border border-[#ddd9cf] bg-white p-4"><p className="text-xs text-black/40">Page size</p><p className="mt-1 text-xl font-black">{result.width} × {result.height}</p></div><div className="rounded-2xl border border-[#ddd9cf] bg-white p-4"><p className="text-xs text-black/40">Orientation</p><p className="mt-1 text-xl font-black">{result.orientation}</p></div><div className="rounded-2xl border border-[#ddd9cf] bg-white p-4"><p className="text-xs text-black/40">Text</p><p className="mt-1 text-xl font-black">{result.textLength}</p></div></div>}</div>}</div>;
}