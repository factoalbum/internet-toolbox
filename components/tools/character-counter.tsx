"use client";
import { useMemo, useState } from "react";
export default function CharacterCounter(){const [text,setText]=useState("");const stats=useMemo(()=>({characters:[...text].length,withoutSpaces:[...text].filter(c=>!/\s/.test(c)).length,lines:text?text.split(/\r?\n/).length:0}),[text]);return <div className="border border-[#d8d4c9] bg-[#fffdf8] p-5 md:p-8"><label className="block"><span className="text-sm font-semibold">Enter your text</span><textarea value={text} onChange={e=>setText(e.target.value)} rows={10} placeholder="Type or paste text here..." className="mt-2 w-full resize-y rounded-md border border-[#bcb8ae] bg-white p-4 text-base leading-7 outline-none focus:border-[#171717]"/></label><div className="mt-5 grid gap-3 sm:grid-cols-3">{[["Characters",stats.characters],["Without spaces",stats.withoutSpaces],["Lines",stats.lines]].map(([label,value])=><div key={label as string} className="border border-[#d8d4c9] bg-[#f3f0e8] p-4"><p className="text-xs font-bold uppercase tracking-[0.12em] text-black/45">{label}</p><p className="mt-1 text-2xl font-black">{value}</p></div>)}</div><p className="mt-5 text-xs leading-5 text-black/45">Counting happens in your browser. No text is uploaded.</p></div>}
